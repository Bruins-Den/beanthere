import React, { Fragment } from 'react';
import Header from '../../components/Header';

const Create = () => {
	return (
		<Fragment>
			<Header />
			<div>Create Page</div>
		</Fragment>
	);
};

export default Create;
