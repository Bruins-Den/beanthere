import React, { Fragment } from 'react';
import Header from '../../components/Header';

const Detail = () => {
	return (
		<Fragment>
			<Header />
			<div>Detail Page</div>
		</Fragment>
	);
};

export default Detail;
